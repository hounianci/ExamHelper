分时系统的四个特征是：
多路性、独立性、及时性、交互性。

基于分页的虚拟存储系统中，常采用两种置换策略：
全局置换、局部置换

为了实现实时调度，系统需要为调度程序提供哪些信息：
就绪时间、开始截止时间、完成截止时间、处理时间、资源要求、优先级。

在内存管理中，分页管理和分段管理的主要区别是什么：
1.页是按物理单位划分的；而段是按逻辑单位划分的。
2.页的大小是固定的，而段的大小不固定。
3.分页的地址是一维的，而分段的地址空间是二维的。

SPOOLing系统的优点：
1.提高了I/O速度
2.将独占设备改造为共享设备
3.实现了虚拟设备功能

